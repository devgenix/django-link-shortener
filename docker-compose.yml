version: '3.8'

services:
  web:
    build: .
    command: gunicorn --bind 0.0.0.0:8000 core.wsgi:application
    volumes:
      - .:/app
    ports:
      - "80:8000"
    environment:
      - DEBUG=0
      - ALLOWED_HOSTS=*
